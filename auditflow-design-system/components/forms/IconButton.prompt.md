Square icon-only button for toolbars, table rows, and dismiss actions. Always pass `label`.

```jsx
<IconButton label="Filter findings" variant="secondary"><FilterIcon/></IconButton>
```

Variants mirror Button (`primary`/`secondary`/`ghost`/`danger`); sizes `sm`/`md`/`lg` render 30/38/46px squares.
