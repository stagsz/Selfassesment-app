Circular avatar; shows the user's photo or initials derived from `name`.

```jsx
<Avatar name="Dana Okoye" size="md" />
<Avatar name="QHSE" src="/team/dana.jpg" />
```

Sizes `xs`/`sm`/`md`/`lg`. Tones `brand`/`slate`/`pass`.
