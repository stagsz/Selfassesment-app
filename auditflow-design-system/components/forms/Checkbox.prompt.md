Checkbox with optional description line; use for evidence checklists and multi-select filters.

```jsx
<Checkbox label="Documented procedure exists" description="Clause 7.5 — controlled document" defaultChecked />
```

Controlled via `checked` + `onChange`, or uncontrolled via `defaultChecked`.
