Radial readiness gauge — the dashboard's headline metric.

```jsx
<ScoreGauge value={94} label="Audit readiness" size={140} />
```

Auto-colors by threshold (≥85 green / ≥60 amber / else red). Pass `tone` to override.
